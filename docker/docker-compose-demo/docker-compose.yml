services:
  nginx:
    image: nginx
    container_name: nginx-compose
    networks:
    - devops-network

  ubuntu:
    image: ubuntu
    container_name: ubuntu-compose
    command: sleep infinity
    networks:
      - devops-network

networks:
  devops-network:
    driver: bridge
